import 'package:flutter/material.dart';

class ConversationsEmpty extends StatelessWidget {
  const ConversationsEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Conversations empty'));
  }
}
